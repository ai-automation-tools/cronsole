{
  "//": "Copy this file to appsettings.json and fill in pairingSecret. Environment variables (TASKHUB_SERVER_URL / TASKHUB_PAIRING_SECRET / TASKHUB_AGENT_ID) override these values. appsettings.json is gitignored so the secret is not committed.",
  "serverUrl": "http://localhost:3000",
  "pairingSecret": "REPLACE_WITH_BACKEND_AGENT_PAIRING_SECRET",
  "agentId": ""
}
