{
  "name": "cronsole-mcp-server",
  "version": "1.0.0",
  "description": "Model Context Protocol server for Cronsole — a thin wrapper over the REST API so Claude / Codex / Cursor can list, run, and create scheduled tasks via natural language.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cronsole-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cronsole",
    "scheduler"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "axios": "^1.16.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
