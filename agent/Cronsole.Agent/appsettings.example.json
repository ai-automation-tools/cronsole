{
  "//": "Copy this file to appsettings.json and fill in pairingSecret. Environment variables (CRONSOLE_SERVER_URL / CRONSOLE_PAIRING_SECRET / CRONSOLE_AGENT_ID) override these values. appsettings.json is gitignored so the secret is not committed.",
  "serverUrl": "http://localhost:3000",
  "pairingSecret": "REPLACE_WITH_BACKEND_AGENT_PAIRING_SECRET",
  "agentId": ""
}
