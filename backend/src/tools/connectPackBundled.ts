/**
 * GENERATED FILE — do not edit.
 *
 * Source: src/tools/connect-pack/
 * Regenerate: npm run connectpack:build
 *
 * Bundled rather than read from disk because tsc emits only .js, so the markdown
 * sources never reach dist/. Guarded by connectPack.drift.test.ts.
 */

export const CONNECT_PACK_FILES: Record<string, string> = {
  "AGENTS.md": "# TaskHub — operating rules for an AI assistant\n\n> TaskHub Connect Pack **v1.1** · canonical copy: <https://taskhub.mikesailab.com>\n\nPaste this into your tool's system prompt, `AGENTS.md`, `CLAUDE.md`, custom instructions, or\nwhatever single-file convention it uses. It is the condensed form of the full skill in this\npack — use the full version if your tool supports skills.\n\n---\n\nYou can create and manage **real scheduled tasks** on this machine through TaskHub — Windows\nTask Scheduler tasks, and TaskHub-native HTTP jobs run by its backend.\n\n**You are scheduling code to run on someone's computer unattended, forever, with nobody\nwatching.** A scheduled task fails differently from an interactive one: no console to print\nto, no user to answer a prompt, nobody to notice for weeks.\n\n## Reaching TaskHub\n\nUse the `taskhub` MCP tools if they are present. Otherwise call the REST API at\n`http://localhost:3000/api` with `Authorization: Bearer <token>`.\n\nIf a tool you expect is missing — notably `delete_task` — that is a deliberate gate the human\nset, not an obstacle. Say so and offer the safe alternative. Do not route around it.\n\n## Non-negotiable rules\n\n1. **Schedules are 5-field cron in UTC.** Not local time. Convert on the way in.\n2. **Always validate a schedule before creating** (`convert_schedule` / `POST /api/tasks/preview`)\n   and **read the returned trigger, not the confidence score.**\n3. **An unrecognized cron is replaced, not approximated.** `0 4 1 1 *` (once a year) becomes an\n   hourly trigger — 8,760 runs instead of 1. The fallback only ever runs **more** often than\n   asked. Recognized shapes: daily, weekly, monthly, `*/N` minutes, `0 */N` hours.\n4. **Never encode \"don't run\" in the cron.** To park a task, **disable** it\n   (`set_task_status`). That verb is ungated precisely so the safe move is the easy one.\n5. **Commands are tokenized with no shell** — `{executable, args[]}`. Pipes, `>`, `&&`, and\n   `%VAR%` do not work unless you invoke a shell explicitly (`cmd.exe /c \"…\"`,\n   `powershell.exe -NoProfile -Command \"…\"`). That explicitness is the security guarantee.\n6. **The command must terminate.** No prompts, no interactive logins, no confirmations —\n   an unattended run hangs forever and reports nothing useful.\n7. **`\\Microsoft\\` is refused.** Registering a task silently overwrites a same-named one and\n   the agent runs elevated; writing there could destroy a real Windows task.\n8. **A folder must already exist.** The only folder TaskHub creates is `\\TaskHub`. Use\n   `list_folders` — never guess a path. `\\TaskHub` being absent from the listing is normal\n   (created on demand, pruned when empty).\n9. **Use `-NoProfile` on PowerShell** and `Invoke-RestMethod` (or `-UseBasicParsing`) for HTTP.\n10. **`update_task_action` replaces the action, it does not patch it.** Read current values\n    before changing one field.\n\n## Verifying\n\nTaskHub reporting `SUCCESS` means the task **started**, not that the command worked — a hung\ncommand reports `SUCCESS`. An empty run history means *TaskHub has no record*, not *it never\nran*.\n\nVerify from outside TaskHub:\n\n```powershell\nGet-ScheduledTaskInfo -TaskPath '\\TaskHub\\' -TaskName '<name>' |\n  Select-Object LastRunTime, LastTaskResult\n#   0      = exited cleanly\n#   267009 = still running (hung)\n```\n\nBest evidence is a real side effect — a log line, a file, an HTTP hit.\n\n**Delete your test tasks.** A test task is a real scheduled task and will fire forever.\n\n## Backing up\n\n`POST /api/tools/export/tasks` with `{\"scope\":\"all\",\"format\":\"zip\"}` exports every task on the\nmachine as native Task Scheduler XML — including tasks TaskHub never imported. The XML is\nUTF-16 LE with a BOM (the only encoding Windows re-imports), so handle it as raw bytes.\n",
  "README.md": "# TaskHub Connect Pack\n\n**v1.1** · canonical copy: <https://taskhub.mikesailab.com>\n\nTeach any AI tool — Claude Code, Claude Desktop, Cursor, Codex, or your own agent — to create\nand manage scheduled tasks through **your** running TaskHub.\n\nEverything here describes how to **use** TaskHub. None of it requires the TaskHub source code.\n\n## What's in the pack\n\n| File | For |\n|:---|:---|\n| `SKILL.md` | Hosts that support skills (Claude Code, Claude Desktop). Drop the whole folder in and the tool loads it when the work matches. |\n| `references/task-authoring.md` | The long form: every creation path, command recipes per kind of work, quoting, folders, the schedule traps, how to verify. |\n| `AGENTS.md` | Hosts with **no** skill concept. One self-contained file to paste into a system prompt, `AGENTS.md`, `CLAUDE.md`, or custom instructions. |\n| `mcp-config.json` | The MCP server wiring, with the traps documented inline. |\n\n## Install\n\n### 1. Wire up the MCP server (recommended)\n\nMerge the `taskhub` block from `mcp-config.json` into your host's MCP config, then:\n\n```bash\ncd <path-to-taskhub>/mcp-server && npm run build\n```\n\nExport your token in the environment you launch the host from:\n\n```powershell\n# Windows — then open a FRESH terminal, environments are inherited at process start\n[Environment]::SetEnvironmentVariable('TASKHUB_TOKEN', '<jwt>', 'User')\n```\n\n```bash\n# POSIX\nexport TASKHUB_TOKEN='<jwt>'\n```\n\nVerify: ask your tool to list your scheduled tasks. If the `taskhub` tools are **missing**\nrather than erroring, the token was unset — the server refuses to start rather than send a\nliteral `${TASKHUB_TOKEN}` and get back a misleading `403 Invalid or expired token`.\n\n**No MCP support in your tool?** Skip this step. The REST API at `http://localhost:3000/api`\ndoes everything, with `Authorization: Bearer <token>`.\n\n### 2. Install the instructions\n\n**Claude Code / Claude Desktop** — copy the skill into your skills directory:\n\n```\n.claude/skills/taskhub/SKILL.md\n.claude/skills/taskhub/references/task-authoring.md\n```\n\n(Or `~/.claude/skills/taskhub/` to make it available everywhere.)\n\n**Cursor, Codex, Windsurf, custom agents** — paste `AGENTS.md` into the tool's instruction\nfile or system prompt. Keep `references/task-authoring.md` nearby for when you need detail.\n\n**Your own agent** — `AGENTS.md` is written to work as a system prompt as-is.\n\n## Before you let a tool loose on this\n\nTaskHub creates **real** scheduled tasks that run unattended and, on Windows, elevated. The\npack front-loads the rules that matter — schedules are UTC cron, an unrecognized cron is\n*replaced* rather than approximated, commands are tokenized with no shell, `\\Microsoft\\` is\nrefused, disable rather than delete — because an assistant without them creates wrong tasks\nconfidently.\n\n`delete_task` is **off by default** and absent from the tool list entirely unless you set\n`TASKHUB_MCP_ALLOW_DESTRUCTIVE=true`. Leave it off unless you have a reason.\n\n## Keeping it current\n\nThis pack is version-stamped. It was generated by your TaskHub install, so it matches the\nversion you are running today — but a copy on another machine will not update itself. If this\npack and your TaskHub disagree, **TaskHub is right**: re-download the pack from the Tools tab.\n",
  "SKILL.md": "---\nname: taskhub\ndescription: Create, run, and manage scheduled tasks through a running TaskHub instance — Windows Task Scheduler tasks and TaskHub-native HTTP jobs. Use when the user asks to schedule something, automate a recurring job, check whether a scheduled job ran, or change/disable/delete an existing scheduled task.\n---\n\n# TaskHub\n\n> TaskHub Connect Pack **v1.1** · canonical copy: <https://taskhub.mikesailab.com>\n> If this file is older than your TaskHub install, the install wins — re-download the pack.\n\nTaskHub is a single pane of glass for scheduled tasks. It runs **locally** on the user's own\nmachine: a web dashboard, a backend API, and a Windows agent that talks to Task Scheduler.\n\nThis skill teaches you to **use** a running TaskHub. You are driving someone's real computer.\n\n> **The frame:** creating a task is not \"calling an API\", it's **registering code to run on\n> someone's computer forever, with no one watching**. A scheduled task fails differently from\n> an interactive one: there is no console to print to, no user to answer a prompt, and no one\n> to notice for weeks.\n\n---\n\n## 1. How you reach it\n\nTwo surfaces, same backend:\n\n| Surface | Use when |\n|:---|:---|\n| **MCP server** (`taskhub`) | Your host supports MCP. 15 tools, 1:1 with API routes. Preferred. |\n| **REST API** (`http://localhost:3000/api`) | No MCP support, or you need something MCP doesn't expose (template import/export, save-as-template, sync, agent pairing). Bearer token in `Authorization`. |\n\nBoth need the backend running and a token. See `README.md` in this pack for wiring.\n\n**If a tool you expect is missing, that is an answer, not an obstacle.** `delete_task` is\nabsent unless the human set `TASKHUB_MCP_ALLOW_DESTRUCTIVE=true`. Say so and offer the safe\nalternative — **disable** it (stops it running), **untrack** it (removes it from TaskHub but\nleaves it running), or the dashboard. Do not route around a gate.\n\n---\n\n## 2. The tool surface\n\n| Action | MCP tool | REST |\n|:---|:---|:---|\n| List / inspect tasks | `list_tasks` | `GET /api/tasks` |\n| Find a real folder | `list_folders` | `GET /api/tasks/folders` |\n| Run now | `run_task` | `POST /api/tasks/:id/run` |\n| Create from a command | `create_task` | `POST /api/tasks` |\n| Create an HTTP job | `create_native_task` | `POST /api/tasks/native` |\n| Create from a template | `create_task_from_template` | `POST /api/templates/:id/apply` |\n| Browse templates | `list_templates` | `GET /api/templates` |\n| Validate a schedule | `convert_schedule` | `POST /api/tasks/preview` |\n| Enable / disable | `set_task_status` | `PATCH /api/tasks/:id/status` |\n| Re-schedule | `update_task_schedule` | `PATCH /api/tasks/:id/schedule` |\n| Edit the command | `update_task_action` | `PATCH /api/tasks/:id/actions` |\n| Run history | `get_task_history` | `GET /api/tasks/:id/executions` |\n| Export one task | `export_task` | `GET /api/tasks/:id/export` |\n| Remove from TaskHub, keep it running | `untrack_task` | `POST /api/tasks/:id/untrack` |\n| Delete | `delete_task` (gated) | `DELETE /api/tasks/:id` |\n| Bulk export / backup | — | `POST /api/tools/export/tasks` |\n| Template import/export, sync, pairing | — | REST / dashboard only |\n\n---\n\n## 3. The invariants — break these and you break the user's machine\n\n| Rule | Why it exists |\n|:---|:---|\n| **Schedules are 5-field cron in UTC** | Not local time. The dashboard converts for display; you convert on the way in. |\n| **Commands are tokenized, no shell** | A Windows command becomes a structured `{executable, args[]}` action. Pipes, `>`, `&&`, `%VAR%` do **not** work unless you invoke a shell explicitly. This is the injection guarantee — an implicit shell turns every parameter into arbitrary code. |\n| **`\\Microsoft\\` is refused** | Registering a task **silently overwrites** a same-named one, and the agent runs **elevated**. Writing there could destroy a real Windows task with no error. Refused in the backend *and* independently in the agent. |\n| **A folder must already exist** | The only folder TaskHub creates is `\\TaskHub` — and the only one it removes. Deleting a folder needs elevation, so it will not create litter it cannot clean up. A create into a missing folder is refused honestly. |\n| **Names are unique per folder** | A collision returns **409** instead of letting Windows silently overwrite. |\n| **Disable is how you park a task** | Never encode \"don't run\" in the cron — see §5. |\n| **Untrack is how you tidy the dashboard** | `untrack_task` removes TaskHub's record and leaves the scheduled task running. Deleting to clean up a view destroys someone's automation. |\n\n---\n\n## 4. Creating a task\n\n**Default to `create_task`** when the user already knows the command. Reach for a template\nwhen they name a *use case* (DB backup, git fetch, webhook), not a command. Do not bend a\nrequest to fit a template.\n\nCommand recipes:\n\n| Kind | Command |\n|:---|:---|\n| PowerShell script | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"C:\\jobs\\nightly.ps1\"` |\n| Executable, direct | `\"C:\\Tools\\backup.exe\" --full --quiet` |\n| HTTP ping | `powershell.exe -NoProfile -Command \"Invoke-RestMethod -Uri 'https://…'\"` |\n| npm / node | `cmd.exe /c \"npm run build > C:\\logs\\build.log 2>&1\"` |\n| Python | `\"C:\\Python312\\python.exe\" \"C:\\jobs\\etl.py\"` |\n| Unattended AI CLI | `powershell.exe -NoProfile -Command \"claude -p 'prompt' --permission-mode dontAsk --allowedTools 'Read,Grep' --bare\"` |\n| HTTP job (no agent) | a URL, with `platform: TASKHUB_NATIVE` |\n\n- `-NoProfile` so an unattended run doesn't depend on the user's shell profile.\n- Quote any path with spaces. Inside `-Command \"…\"`, use single quotes for inner strings.\n- Keep `.ps1` files pure ASCII — one em-dash in a BOM-less script makes PowerShell 5.1 fail\n  to parse it.\n- **The command must terminate.** An unattended run has no console: a prompt, a confirmation,\n  or an interactive login hangs forever and reports nothing.\n\nFull detail: `references/task-authoring.md` in this pack.\n\n---\n\n## 5. The schedule trap — read this one twice\n\n**Always `convert_schedule` first, and read the returned trigger, not the score.**\n\n| Score | Means |\n|:---|:---|\n| `1.0` | Exact native trigger. **Still read it.** A multi-day cron once scored 1.0 with no warnings while silently dropping four of five days. |\n| `0.7` | Lossy **or replaced** — two different things sharing one score. Branch on the `lossy` field: `'approximated'` = derived from your input but drifts; `'replaced'` = your input was **discarded** for a hard-coded hourly trigger. |\n| `0` | Not convertible. Fix the expression. |\n\n> **A \"rare\" cron is the dangerous one.** `0 4 1 1 *` (once a year) is not recognized, so it\n> is replaced with a trigger that repeats **hourly** — 8,760 runs a year instead of 1. The\n> fallback only ever runs **more** often than you asked, never less.\n\n**To create a task that shouldn't fire yet:** use a schedule the converter recognizes and\n**disable** it. Never reach for an exotic cron to mean \"never\".\n\nRecognized shapes: daily (`0 9 * * *`), weekly (`0 9 * * 1-5`), monthly (`0 3 1 * *`),\nminute step (`*/15 * * * *`), hour step (`0 */4 * * *`). Everything else hits the fallback.\n\n---\n\n## 6. Verifying — the part everyone skips\n\n**TaskHub's own `SUCCESS` is not proof the command worked.** The agent observes that the task\n*started*, never that it finished correctly — so a command that hangs forever reports\n`SUCCESS`. An empty run history means *TaskHub has no record*, not *it never ran*: a Windows\ntask firing on its own trigger is recorded by Windows, not by TaskHub.\n\n> **When you are testing whether the reporting is honest, the reporting cannot be your\n> witness.** Get evidence from outside TaskHub:\n\n```powershell\n$t = Get-ScheduledTask -TaskPath '\\TaskHub\\' -TaskName '<name>'\n$t.Actions  | Select-Object Execute, Arguments      # direct exec? no stray cmd.exe /c?\n$t.Triggers | Select-Object StartBoundary, Repetition   # UTC -> local correct?\nStart-ScheduledTask -InputObject $t\nGet-ScheduledTaskInfo -InputObject $t | Select-Object LastRunTime, LastTaskResult\n#   0      = exited cleanly           <- Windows' own record, independent of TaskHub\n#   267009 = STILL RUNNING (hung)     <- flat CPU means blocked, not working\n```\n\nBest evidence is a **real side effect** — a log line, a file, an HTTP hit.\n\n**Clean up test tasks.** A test task is a real scheduled task; it fires forever if you leave it.\n\n---\n\n## 7. Before you create anything on a real machine\n\n1. `convert_schedule` the cron — and **read the trigger**, not the score.\n2. Cron is **UTC**, 5 fields.\n3. The command tokenizes the way you intend; a shell is **explicit** if you need one.\n4. Folder **exists** (`list_folders`) — or omit it and take the default `\\TaskHub`.\n   Its absence from the listing is normal: it is created on demand and pruned when empty.\n5. The name won't collide in that folder.\n6. The command **terminates**.\n7. After creating: run it and **verify from outside TaskHub**.\n8. If it was a test: delete it.\n",
  "mcp-config.json": "{\n  \"//\": [\n    \"TaskHub Connect Pack v1.1 - MCP server wiring.\",\n    \"Newer copy: https://taskhub.mikesailab.com\",\n    \"\",\n    \"Merge the `taskhub` entry below into your host's MCP config:\",\n    \"  Claude Code  ->  .mcp.json in your project root, or ~/.claude.json\",\n    \"  Claude Desktop -> claude_desktop_config.json\",\n    \"  Cursor / Codex / others -> whatever file that host documents; the shape is the same.\",\n    \"\",\n    \"Replace <PATH-TO-TASKHUB> with where you installed TaskHub. The MCP server runs\",\n    \"dist/, not src/ - run `cd mcp-server && npm run build` once before first use, and\",\n    \"again after any update, or your change is invisible.\",\n    \"\",\n    \"TASKHUB_TOKEN is a ${ENV} reference on purpose so this file never holds the secret.\",\n    \"Export it in the environment you launch the host from. On Windows a newly set\",\n    \"variable needs a FRESH terminal - a process inherits its parent's environment, so\",\n    \"restarting the host inside an old terminal will not pick it up.\",\n    \"\",\n    \"If TASKHUB_TOKEN is unset, the literal text ${TASKHUB_TOKEN} is forwarded and the API\",\n    \"answers 403 Invalid or expired token - which reads like an expired JWT and sends you\",\n    \"debugging auth instead of your environment. The server detects that and refuses to\",\n    \"start, so the tools go MISSING rather than erroring.\",\n    \"\",\n    \"TASKHUB_MCP_ALLOW_DESTRUCTIVE is omitted deliberately. Leaving it off keeps the\",\n    \"irreversible delete_task tool ABSENT from the tool list. The reversible verbs\",\n    \"(set_task_status, update_task_schedule, update_task_action) are always available -\",\n    \"prefer disabling a task over deleting it.\"\n  ],\n  \"mcpServers\": {\n    \"taskhub\": {\n      \"type\": \"stdio\",\n      \"command\": \"node\",\n      \"args\": [\"<PATH-TO-TASKHUB>/mcp-server/dist/index.js\"],\n      \"env\": {\n        \"TASKHUB_API_URL\": \"http://localhost:3000/api\",\n        \"TASKHUB_TOKEN\": \"${TASKHUB_TOKEN}\"\n      }\n    }\n  }\n}\n",
  "references/task-authoring.md": "# Task authoring & management\n\n> TaskHub Connect Pack **v1.1** · canonical copy: <https://taskhub.mikesailab.com>\n\nEvery way to **create** a scheduled task through TaskHub, and how to **manage** it afterwards.\nRead this before creating a task on a user's real machine — a scheduled task is durable, runs\nunattended, and on Windows runs **elevated**.\n\n---\n\n## 1. Pick the creation path\n\n| You have… | Use | Why |\n|:---|:---|:---|\n| **A command you already know** | `create_task` / `POST /api/tasks` | The direct path. A template is the wrong unit when you already know the command. |\n| **A recognizable use case** (DB backup, git fetch, webhook) | `create_task_from_template` / `POST /api/templates/:id/apply` | Inherits a tested command, a sane default schedule, declared parameters. Call `list_templates` first for the id and its params. |\n| **A URL to poll** | `create_task` with `platform: TASKHUB_NATIVE` | Becomes a backend-run HTTP GET job. **No agent, no Windows task** — so it works with the agent offline. The command must be a URL; anything else is refused. |\n| **A richer HTTP job** (non-GET, headers, body) | `create_native_task` / `POST /api/tasks/native` | Takes a full job spec. Separate from `create_task` because the command-string path only builds a GET. |\n| **A task that already exists and is good** | `POST /api/tasks/:id/save-as-template` | Turns a real task into a reusable template. |\n| **A template JSON from the gallery** | `POST /api/templates/import` | Same schema and placeholder validation as any catalog content. |\n\n---\n\n## 2. Compose the command (Windows)\n\nA Windows `command` is **tokenized into a structured no-shell action** — `{executable,\nargs[]}` — before the agent registers it:\n\n```\ncreate_task command: 'powershell.exe -NoProfile -File \"C:\\jobs\\nightly.ps1\"'\n       ->  Execute:   powershell.exe\n           Arguments: -NoProfile -File C:\\jobs\\nightly.ps1     # one arg, quotes consumed\n```\n\n**Consequence: pipes, `>`, `&&`, and `%VAR%` do NOT work by default** — there is no shell to\ninterpret them. Invoke one explicitly when you need it:\n\n```powershell\ncmd.exe /c \"npm outdated > C:\\reports\\deps.txt\"       # redirection needs a shell\npowershell.exe -NoProfile -Command \"...\"               # PowerShell features need PowerShell\n```\n\nThat explicitness is the point: a shell you opted into is auditable; an implicit one turns\nevery parameter into arbitrary code.\n\n### Recipes by kind of work\n\n| Kind | Command | Notes |\n|:---|:---|:---|\n| **PowerShell script** | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"{path}.ps1\"` | `-NoProfile` so an unattended run doesn't depend on the user's profile. |\n| **Executable, direct** | `\"C:\\Tools\\backup.exe\" --full --quiet` | The cleanest case. Quote the exe if its path has spaces. |\n| **HTTP ping** | `powershell.exe -NoProfile -Command \"Invoke-RestMethod -Uri 'https://…'\"` | Prefer `Invoke-RestMethod`. If you must use `Invoke-WebRequest`, **`-UseBasicParsing` is mandatory** — without it, it can block waiting on Internet Explorer engine initialization in a session that has no user. |\n| **npm / node** | `cmd.exe /c \"npm run build > C:\\logs\\build.log 2>&1\"` | `npm` is a `.cmd`, and redirection needs a shell — so the shell is explicit. |\n| **Python** | `\"C:\\Python312\\python.exe\" \"C:\\jobs\\etl.py\"` | Prefer the absolute interpreter path over relying on PATH under Task Scheduler. |\n| **Docker** | `docker compose -f \"{composeFile}\" up -d` | Direct exec; docker is a real exe on PATH. |\n| **Unattended AI CLI** | `powershell.exe -NoProfile -Command \"claude -p '{prompt}' --permission-mode dontAsk --allowedTools 'Read,Grep' --bare\"` | Agent runs **must** be non-interactive or they hang forever on an approval prompt. |\n| **Native HTTP job** | `https://example.com/health` with `platform: TASKHUB_NATIVE` | Backend-executed, no agent required. |\n\n### Quoting\n\n- Quote any path with spaces: `-File \"C:\\Program Files\\x\\job.ps1\"`.\n- Inside `powershell.exe -Command \"…\"`, use **single quotes** for inner strings so the whole\n  script rides as one double-quoted argument.\n- Keep `.ps1` / `.vbs` files **pure ASCII**. A single em-dash in a BOM-less script makes\n  PowerShell 5.1 fail to parse it.\n\n---\n\n## 3. Get the schedule right\n\n**All schedules are 5-field cron in UTC.** Not local time.\n\n**Always `convert_schedule` first — and read the returned `trigger`, not the score.**\n\n```\nconvert_schedule '0 9 * * 1-5'\n  -> score 1, Weekly at 09:00 on Monday, Tuesday, Wednesday, Thursday, Friday  <- verify the days\n```\n\n| Score | Means | Do |\n|:---|:---|:---|\n| **1.0** | Native trigger, exact | Still read it. A multi-day cron once scored 1.0 with no warnings while dropping four of five days. |\n| **0.7** | Lossy **or replaced** — two very different things sharing one score | Branch on the machine-readable `lossy` field, not the number. `'approximated'` = the trigger *is* derived from your input and merely drifts. `'replaced'` = your input was **discarded** for a hard-coded hourly trigger; delete and re-create with a recognized shape. |\n| **0** | Invalid / not convertible | Fix the expression. |\n\n> **A \"rare\" cron is the dangerous one.** `0 4 1 1 *` (once a year) registers as **daily,\n> repeating hourly** — 8,760 runs a year. The fallback only ever runs **more** often than you\n> asked, never less. **Never encode \"don't run\" in the cron.**\n\n**To create a task that shouldn't fire yet:** use a recognized schedule and **disable** it, or\ncreate it and delete it when done.\n\nRecognized shapes: daily (`0 9 * * *`), weekly (`0 9 * * 1-5`), monthly (`0 3 1 * *`),\nminute step (`*/15 * * * *`), hour step (`0 */4 * * *`). Everything else hits the fallback.\n\n---\n\n## 4. Placement, naming, and the refusals\n\n| Rule | Behavior |\n|:---|:---|\n| **Default folder is `\\TaskHub`** | The only folder TaskHub creates — and the only one it prunes when the last task leaves. |\n| **Any other folder must ALREADY EXIST** | Deleting a folder needs elevation, so TaskHub will not create one it cannot remove. A create into a missing folder is refused honestly. |\n| **`\\Microsoft\\` is refused outright** | Registering a task **silently overwrites** a same-named one, and the agent runs **elevated** — writing there could destroy a real Windows task with no error. Refused in the backend **and** independently in the agent. |\n| **Name must be unique per folder** | A collision returns **409** rather than letting Windows silently overwrite. `\\Work\\Backup` and `\\TaskHub\\Backup` are different tasks. |\n\n**Use `list_folders` to find a valid one.** It returns every real folder with its task count\nand whether you can create there. Two things about that listing that will otherwise mislead\nyou:\n\n- **An unwritable folder is still listed** (`writable: false`, e.g. `\\Microsoft\\…`). That is\n  deliberate — *\"exists but refused\"* is a different fact from *\"doesn't exist\"*. Do not read\n  its presence as permission.\n- **The default `\\TaskHub` is often absent.** It is created lazily and **pruned when its last\n  task is deleted**, so on a clean machine it genuinely does not exist yet. That is not a\n  problem and not a reason to pick another folder — omit `folder` and TaskHub creates it.\n  `list_folders` reports `defaultFolder` separately for exactly this reason.\n\n---\n\n## 5. Managing a task afterwards\n\n| Action | MCP | REST |\n|:---|:---|:---|\n| List / inspect | `list_tasks` | `GET /api/tasks` |\n| Find a folder | `list_folders` | `GET /api/tasks/folders` |\n| Run now | `run_task` | `POST /api/tasks/:id/run` |\n| Create | `create_task`, `create_native_task`, `create_task_from_template` | `POST /api/tasks`, `/native`, `/api/templates/:id/apply` |\n| Validate a schedule | `convert_schedule` | `POST /api/tasks/preview` |\n| **Enable / disable** | `set_task_status` | `PATCH /api/tasks/:id/status` |\n| **Re-schedule** | `update_task_schedule` | `PATCH /api/tasks/:id/schedule` |\n| **Edit the command** | `update_task_action` | `PATCH /api/tasks/:id/actions` |\n| Run history | `get_task_history` | `GET /api/tasks/:id/executions` |\n| Export one task | `export_task` | `GET /api/tasks/:id/export` |\n| Bulk export / backup | — | `POST /api/tools/export/tasks` |\n| **Untrack** (remove from TaskHub, keep it running) | `untrack_task` | `POST /api/tasks/:id/untrack` |\n| **Delete** | `delete_task` — **only** when the human set `TASKHUB_MCP_ALLOW_DESTRUCTIVE=true`, else absent | `DELETE /api/tasks/:id` |\n\n**Disable is how you park a task** — not a weird cron (§3 explains why that backfires). It is\nreversible and ungated precisely so the safe move is the easy one.\n\n**Untrack is how you tidy the dashboard** — not delete. `untrack_task` removes TaskHub's record of\na task (and its TaskHub run history) while leaving the real scheduled task exactly where it is,\nstill running on its own schedule; future syncs won't pull it back. Reach for it when someone\nimported a folder by mistake, or wants OS-owned tasks out of their view. It refuses TaskHub-native\ntasks, which exist only inside TaskHub and so have nothing to keep.\n\n**Three verbs, three blast radii — never substitute one for another:**\n\n| Goal | Verb | What survives |\n|:---|:---|:---|\n| Stop it running, keep everything | `set_task_status: DISABLED` | the task, its schedule, its history |\n| Stop *seeing* it, keep it running | `untrack_task` | the real scheduled task |\n| Make it stop existing | `delete_task` (gated) | nothing |\n\n**If `delete_task` isn't in your tool list, that is the answer.** Say so and offer\n`set_task_status: DISABLED`, the dashboard, or the REST call. Don't route around it. Delete\nremoves the real Task Scheduler entry, and TaskHub's own record goes **only after the platform\nconfirms**; a task with an admin ACL gets an honest \"needs elevation\" refusal rather than a\nfake success.\n\nTwo behaviors worth knowing before you use these:\n\n- **`update_task_action` REPLACES the action, it does not patch it.** `command` and `runLevel`\n  are both required. Read the task's current values first, or you will silently reset the one\n  you didn't mean to change.\n- **`get_task_history` is not a complete record.** TaskHub logs manual runs it triggered and\n  its own native jobs; a Windows task firing on its **own** trigger is recorded by Windows. An\n  empty history means \"TaskHub has nothing\", never \"it never ran\".\n\n---\n\n## 6. Verify it — the part everyone skips\n\n**A command that tokenizes is not a command that runs.** Neither is TaskHub's own\n`lastRunStatus: SUCCESS` — the agent observes that the task **started**, never that the\ncommand worked, so a command that hangs forever reports `SUCCESS`.\n\n> **When you're testing whether the reporting is honest, the reporting cannot be your\n> witness.** Get evidence from outside TaskHub.\n\n```powershell\n$t = Get-ScheduledTask -TaskPath '\\TaskHub\\' -TaskName '<name>'\n$t.Actions  | Select-Object Execute, Arguments\n$t.Triggers | Select-Object StartBoundary, DaysInterval, Repetition\nStart-ScheduledTask -InputObject $t\nGet-ScheduledTaskInfo -InputObject $t | Select-Object LastRunTime, LastTaskResult\n#   0      = exited cleanly\n#   267009 = STILL RUNNING (hung)\n```\n\nBest evidence is a **real side effect** — a log line, a file, an HTTP hit. If the command\ncan't prove it ran, you haven't verified it.\n\n**Clean up after a test.** A test task is a real scheduled task: it will keep firing forever\nif you leave it.\n\n---\n\n## 7. Backing tasks up\n\nBefore a risky change — and on a schedule, ideally — export the machine's tasks:\n\n```\nPOST /api/tools/export/tasks\n  { \"scope\": \"all\",    \"format\": \"zip\" }\n  { \"scope\": \"folder\", \"folder\": \"\\\\Work\", \"format\": \"zip\" }\n```\n\nThis exports what is **on the machine**, not only what TaskHub imported, as native Task\nScheduler XML. The XML is UTF-16 LE with a BOM — the only encoding Windows re-imports — so if\nyou write it anywhere, write it as **raw bytes**, never re-encoded as UTF-8. Tasks under\n`\\Microsoft\\` are excluded unless you pass `includeSystem: true`.\n\nRe-import on Windows with `Register-ScheduledTask -Xml (Get-Content -Raw file.xml)`.\n\n---\n\n## 8. Checklist\n\nBefore you call `create_task` on a real machine:\n\n1. **`convert_schedule`** the cron — and **read the trigger**, not the score.\n2. Cron is **UTC**, 5 fields.\n3. The command **tokenizes** the way you intend; a shell is **explicit** if you need one.\n4. `-NoProfile` on PowerShell; `Invoke-RestMethod` (or `-UseBasicParsing`) for HTTP.\n5. Folder **exists and is writable** — check with `list_folders`, don't guess. Or omit it and\n   take the default `\\TaskHub`.\n6. The name won't collide in that folder.\n7. The command **terminates**. An unattended run has no console.\n8. After creating: **run it and verify from outside TaskHub**.\n9. If it was a test: **delete it**.\n"
};
