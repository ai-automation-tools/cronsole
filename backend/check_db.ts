import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const count = await prisma.task.count(); console.log('Task count:', count); } main().catch(console.error).finally(() => prisma.\());
